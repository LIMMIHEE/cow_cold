name: cow_cold
description: 글을 읽고 느낀 감정에 대한 기록을 남기는 앱 프로젝트

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'


dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  get:
  firebase_core:
  firebase_database:
  cloud_firestore:
  flutter_dotenv:
  shared_preferences:
  firebase_auth:
  flutter_svg:
  crypto:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/.env
    - assets/images/
